package model;

/** The code snippet is defining an enumeration called `TypeVisit` with two possible values: `VISITED`
 and `NOT_VISITED`. This enum can be used to represent the status of a visit, where `VISITED`
 indicates that a visit has occurred and `NOT_VISITED` indicates that a visit has not yet occurred.*/ 
public enum TypeVisit {
    VISITED, NOT_VISITED;
}